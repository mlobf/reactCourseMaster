const myArray = [1, 2, 3, 4, 5];

//-------------------------------------------------------------
console.log("How to use Map in JS");
// For each element in array will be replace by
// 	the return off the function that is inside of
//		map method.
newArray = myArray.map((el) => el * el);
console.log(newArray);
//-------------------------------------------------------------

console.log("How to use Promisses in JS");

//-------------------------------------------------------------
